<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 设置背景色 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="0.0dip" />

            <gradient
                android:endColor="@color/progress_bj_new"
                android:startColor="@color/progress_bj_new" />
        </shape>
    </item>

    <!-- 设置进度条颜色 -->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="0.0dip" />

                <gradient
                    android:endColor="@color/first_theme"
                    android:startColor="@color/first_theme" />
            </shape>
        </clip>
    </item>

</layer-list>